def hello_function(greeting, name): #los elementos que estan dentro de los () son las variables de la funcion
    return 'Hello Function.' #como salida de una funcio conviene usar l funcion 'return'
    #porque no sabemos si siempre vamos a querer que al ejecutar una funcion se imprima el resultado
    #algunas veces podemos querer que ese resultado sea tomado por otra funcion sin que se imprima

print(hello_function())
print(hello_function())
